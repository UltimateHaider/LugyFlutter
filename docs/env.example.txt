# LugyFlutter - نموذج ملف الإعدادات
# =============================
# انسخ هذا الملف إلى .env وقم بتعديل القيم

# إعدادات Google Gemini API (مطلوب)
GOOGLE_API_KEY=your_actual_api_key_here

# إعدادات المتصفح
CHROME_USER_DATA_DIR=
BROWSER_HEADLESS=false
BROWSER_TIMEOUT=30

# إعدادات LugyFlutter
LOG_LEVEL=INFO
MAX_STEPS=30
RETRY_DELAY=3
SAVE_SCREENSHOTS=true

# إعدادات الويب
WEB_DASHBOARD_ENABLED=true
WEB_DASHBOARD_PORT=8501
API_ENABLED=false
API_PORT=8000

# إعدادات متقدمة (لا تغيرها إلا إذا كنت خبيراً)
AUTO_HEALING_ENABLED=true
VISION_FALLBACK_ENABLED=true
CONTEXT_MEMORY_SIZE=100
MAX_CHECKPOINTS=20